<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :userid %>
    <%= form.text_field :userid, id: :post_userid %>
  </div>

  <div class="field">
    <%= form.label :msg %>
    <%= form.text_field :msg, id: :post_msg %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.number_field :type, id: :post_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
